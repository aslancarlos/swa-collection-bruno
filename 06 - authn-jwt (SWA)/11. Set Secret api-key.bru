meta {
  name: 11. Set Secret api-key
  type: http
  seq: 11
}

post {
  url: {{SWA_API_BASE}}/api/secrets/conjur/variable/{{SECRET_ID}}
  body: text
  auth: none
}

body:text {
  example-secret-value
}

docs {
  # 11. Set Secret api-key

  POST /api/secrets/conjur/variable/{{SECRET_ID}}  - body = secret value.
}
