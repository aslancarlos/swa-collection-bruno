meta {
  name: List Trust Domains
  type: http
  seq: 2
}

get {
  url: {{SWA_API_BASE}}/api/swa/trust-domains
  body: none
  auth: none
}

assert {
  res.status: eq 200
}

docs {
  # List Trust Domains

  GET /api/swa/trust-domains

  Lists all trust domains in the tenant. Returns count + trust_domains array.
}
