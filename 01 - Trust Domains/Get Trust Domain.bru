meta {
  name: Get Trust Domain
  type: http
  seq: 3
}

get {
  url: {{SWA_API_BASE}}/api/swa/trust-domains/{{TRUST_DOMAIN_NAME}}
  body: none
  auth: none
}

assert {
  res.status: eq 200
}

docs {
  # Get Trust Domain

  GET /api/swa/trust-domains/{trust-domain-name}

  Details of a specific trust domain (env TRUST_DOMAIN_NAME).
}
